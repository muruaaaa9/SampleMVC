describe("Engine", function () {

	it("should not be running when first constructed", function () {
		var motor = new CTM.Engine();
		
	});
	
	it("should be running after start", function () {
		var motor = new CTM.Engine();
		
	});

});